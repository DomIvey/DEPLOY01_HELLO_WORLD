pipeline {
  agent any

  stages {
    stage ('Build') {
      steps {
        sh "echo Run a command"
     }
   }
    stage ('test') {
      steps {
        sh "echo Run a command"
      }
    
    }
  }
}

pipeline {
  agent any

  stages {
    stage ('test') {
      steps {
        sh "echo Run a command"
     }
   }
    stage ('deploy') {
      steps {
        sh "echo Run a command"
      }
    
    }
  }
}


pipeline {
  agent any

  stages {
    stage ('Build') {
      steps {
        sh "echo This is my first build"
     }
   }
    stage ('test') {
      steps {
        sh "echo Run a command. Today is a great day"
      }
    
    }
  }
}
